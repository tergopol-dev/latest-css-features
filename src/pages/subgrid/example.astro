---
import Layout from "../../layouts/Layout.astro";
---

<Layout
  title="HD's Round Table: CSS Subgrid Example"
  mainNavConfiguration={{
    isBackLink: true,
    linkSource: "/subgrid",
    linkTitle: "Back",
  }}
>
  <div
    class="px-4 max-w-screen-2xl mx-auto grid lg:grid-cols-2 gap-12 items-start"
  >
    <h1 class="text-4xl !mb-0">Subgrid Example</h1>
    <!-- Using "grid" class along with "prose" class to fix pre wrapping issue on mobile. GitHub Issue: https://github.com/tailwindlabs/tailwindcss-typography/issues/96#issuecomment-1017139288 -->
    <div class="prose grid lg:row-start-2">
      <pre><p>CSS</p><code is:raw>.subgrid {
  display: grid;
  grid-template-columns: subgrid;
  grid-template-rows: subgrid;
}</code></pre>
      <pre
        class="mt-0"><p>HTML</p><code class="language-html" is:raw>&lt;section class="grid grid-cols-2 lg:grid-cols-3 gap-4 lg:row-start-2"&gt;
  &lt;div class="card-subgrid-example p-6 bg-slate-200 dark:bg-slate-700 grid grid-rows-subgrid row-span-3 gap-4"&gt;
    &lt;p class="font-semibold !text-cyan-500 border-b border-b-slate-500 border-opacity-25 pb-2"&gt;
      Lorem Ipsum
    &lt;/p&gt;
    &lt;div class="text-sm dark:[&&gt;p]:text-slate-300 border-b border-b-slate-500 border-opacity-25 pb-2"&gt;
      &lt;p&gt;
        Nam nec vestibulum ex. Phasellus augue nibh, accumsan vel
        iaculis eu, fermentum nec urna. Sed in enim egestas, auctor
        nisi eu, faucibus felis.
      &lt;/p&gt;
      &lt;p&gt;
        Duis ligula dui, pellentesque dignissim tellus et, luctus
        gravida tellus. Vivamus eleifend nisi sed urna varius, ut
        eleifend arcu interdum.
      &lt;/p&gt;
    &lt;/div&gt;
    &lt;button
      class="rounded bg-slate-300 text-slate-500 dark:text-slate-700 p-2 text-xs font-bold"
      type="button"
    &gt;
      CTA Button
    &lt;/button&gt;
  &lt;/div&gt;
&lt;/section&gt;</code></pre>
    </div>
    <section class="grid grid-cols-2 lg:grid-cols-3 gap-4 lg:row-start-2">
      {
        [...Array(6)].map((_, index) => {
          return (
            <div class="card-subgrid-example p-6 bg-slate-200 dark:bg-slate-700 grid grid-rows-subgrid row-span-3 gap-4">
              <p class="font-semibold !text-cyan-500 border-b border-b-slate-500 border-opacity-25 pb-2">
                {index + 1}. Lorem Ipsum
              </p>
              <div class="text-sm dark:[&>p]:text-slate-300 border-b border-b-slate-500 border-opacity-25 pb-2">
                <p>
                  Nam nec vestibulum ex. Phasellus augue nibh, accumsan vel
                  iaculis eu, fermentum nec urna. Sed in enim egestas, auctor
                  nisi eu, faucibus felis.
                </p>
                {index % 2 === 0 && (
                  <p>
                    Duis ligula dui, pellentesque dignissim tellus et, luctus
                    gravida tellus. Vivamus eleifend nisi sed urna varius, ut
                    eleifend arcu interdum.
                  </p>
                )}
              </div>
              <button
                class="rounded bg-slate-300 text-slate-500 dark:text-slate-700 p-2 text-xs font-bold"
                type="button"
              >
                CTA Button
              </button>
            </div>
          );
        })
      }
    </section>
  </div>
</Layout>
