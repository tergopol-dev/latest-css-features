---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<span
  class:list={[
    "motion-safe:animate-hue-rotate bg-clip-text text-transparent",
    className,
  ]}><slot /></span
>
